

# nodejs
node_modules/

# build output
dist/
build/

# temp
temp
tmp

# log
logs
.log
.log*

# env
.env
*.env

# 링크드인 세션. 액세스 토큰은 60일이면 죽는 파생 상태라 .env 가 아니라 여기 산다
# (make linkedin-auth 가 써넣는다). 토큰이 들어 있으므로 절대 추적하지 않는다.
# CI 는 이 파일 없이 GitHub Secrets 의 환경변수로 동작한다.
.linkedin-session.json

# google service account key
config/google_service_account.json

# OS
.DS_Store

# hugo
hugo/resources/_gen
hugo/public
.claude

# landing (root domain, separate Hugo site)
landing/public
landing/public-develop
# blog 최신/인기글 스냅샷. 배포 때마다 덮어써지는 생성물이라 추적하지 않는다
# (CI: hugo/public/index.json 복사 / 로컬: make landing-data 가 blog에서 받아옴).
# 커밋된 값이 배포에 쓰이는 경로가 없는데 diff에는 보여서 실제로 오해를 샀다.
# 파일이 없어도 landing/layouts/index.html 이 hugo.Data.blog_posts 를 with 로 감싸고 있어
# 해당 섹션만 비고 빌드는 통과한다.
landing/data/blog_posts.json
landing/resources/_gen
landing/.hugo_build.lock

# claude-seo audit output
blog.plzhans.com-audit/
plzhans.com-audit/
